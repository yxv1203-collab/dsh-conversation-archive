# 工作区管理 Host

这是 DeepSeek Harness 的 Cordis Host 插件入口。它以 DSH 原生归档状态为唯一事实来源，负责每会话缓存、AI 保留事务、本地备份、安全 API 与诊断；浏览器界面位于 `client/`。

请从发布包根目录运行 `scripts/install.ps1`，不要单独复制本目录。完整安装、数据位置、隐私和恢复说明见根目录 `README.md`。
